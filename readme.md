## Requirement

`config.yml`

    default:
        NN: 8
        lower_bound: 0.05
        upper_bound: 0.95
        
    database:
        DB_HOST:
        DB_USER:
        DB_PSWD:
        DB_NAME:

To generate report, run

    quarto render report.qmd
